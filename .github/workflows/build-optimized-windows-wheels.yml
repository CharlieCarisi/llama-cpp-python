name: Build optimized Windows wheels

on:
  workflow_call:
    inputs:
      version:
        description: llama-cpp-python release version
        required: false
        type: string
        default: "0.3.34"
  workflow_dispatch:
    inputs:
      version:
        description: llama-cpp-python release version
        required: true
        default: "0.3.34"
permissions:
  contents: read

env:
  SOURCE_VERSION: ${{ inputs.version || '0.3.34' }}
  VULKAN_SDK_VERSION: "1.4.341.0"
  TEST_MODEL_URL: "https://huggingface.co/ggml-org/models-moved/resolve/main/tinyllamas/stories15M-q4_0.gguf?download=true"
  TEST_MODEL_SHA256: "66967fbece6dbe97886593fdbb73589584927e29119ec31f08090732d1861739"

jobs:
  build:
    name: ${{ matrix.variant }}
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: optimized-cpu-tigerlake
            package_suffix: win.srw.cpu.tgl1
            vulkan: "false"
          - variant: optimized-vulkan-irisxe
            package_suffix: win.srw.vulkan.xe1
            vulkan: "true"

    env:
      BUILD_VARIANT: ${{ matrix.variant }}
      ENABLE_VULKAN: ${{ matrix.vulkan }}
      PACKAGE_VERSION: ${{ format('{0}+{1}', inputs.version || '0.3.34', matrix.package_suffix) }}

    steps:
      - name: Set up MSVC x64
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Set up Python 3.9 x64
        uses: actions/setup-python@v6
        with:
          python-version: "3.9"
          architecture: x64

      - name: Install Vulkan SDK
        if: matrix.vulkan == 'true'
        shell: pwsh
        run: |
          choco install vulkan-sdk --version="$env:VULKAN_SDK_VERSION" --no-progress -y
          $sdk = Join-Path "C:\VulkanSDK" $env:VULKAN_SDK_VERSION
          if (-not (Test-Path $sdk)) {
            throw "Vulkan SDK was not found at $sdk"
          }
          "VULKAN_SDK=$sdk" >> $env:GITHUB_ENV
          "$sdk\Bin" >> $env:GITHUB_PATH
          & "$sdk\Bin\glslc.exe" --version

      - name: Install build dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade "pip<26"
          python -m pip install build wheel ninja

      - name: Clone exact upstream release
        shell: pwsh
        run: |
          $tag = "v$env:SOURCE_VERSION"
          git clone --branch $tag --depth 1 --recurse-submodules --shallow-submodules https://github.com/abetlen/llama-cpp-python.git src
          git -C src rev-parse HEAD
          git -C src submodule status

      - name: Apply monolithic layout and Windows SRWLOCK fix
        shell: pwsh
        run: |
          $pythonCmakePath = "src\CMakeLists.txt"
          $pythonCmake = Get-Content $pythonCmakePath -Raw
          $sharedSetting = 'set(BUILD_SHARED_LIBS "On")'
          if (-not $pythonCmake.Contains($sharedSetting)) {
            throw "Expected BUILD_SHARED_LIBS setting was not found"
          }
          $pythonCmake = $pythonCmake.Replace(
            $sharedSetting,
            'set(BUILD_SHARED_LIBS "Off")'
          )

          $ggmlRuntimeInstalls = [regex]'(?ms)\s*install\(\s*FILES \$<TARGET_RUNTIME_DLLS:ggml>\s*DESTINATION [^\r\n]+\s*\)'
          $ggmlRuntimeInstallMatches = $ggmlRuntimeInstalls.Matches($pythonCmake)
          if ($ggmlRuntimeInstallMatches.Count -ne 2) {
            throw "Expected two ggml runtime-DLL install rules, found $($ggmlRuntimeInstallMatches.Count)"
          }
          $pythonCmake = $ggmlRuntimeInstalls.Replace($pythonCmake, "")
          Set-Content $pythonCmakePath $pythonCmake -Encoding UTF8

          $llamaCmakePath = "src\vendor\llama.cpp\src\CMakeLists.txt"
          $llamaCmake = Get-Content $llamaCmakePath -Raw
          $addLlama = [regex]'add_library\(llama\s*\r?\n'
          if (-not $addLlama.IsMatch($llamaCmake)) {
            throw "Expected add_library(llama) declaration was not found"
          }
          $llamaCmake = $addLlama.Replace(
            $llamaCmake,
            "add_library(llama SHARED`r`n",
            1
          )
          Set-Content $llamaCmakePath $llamaCmake -Encoding UTF8

          $threadingPath = "src\vendor\llama.cpp\ggml\src\ggml-threading.cpp"
          $threadingSource = Get-Content $threadingPath -Raw
          $oldThreading = @'
          #include "ggml-threading.h"
          #include <mutex>

          std::mutex ggml_critical_section_mutex;

          void ggml_critical_section_start() {
              ggml_critical_section_mutex.lock();
          }

          void ggml_critical_section_end(void) {
              ggml_critical_section_mutex.unlock();
          }
          '@

          $srwThreading = @'
          #include "ggml-threading.h"

          #define WIN32_LEAN_AND_MEAN
          #include <windows.h>

          // SRWLOCK_INIT is valid as soon as the DLL is mapped and requires
          // no C++ dynamic initializer or process-global CRT constructor.
          static SRWLOCK ggml_critical_section_lock = SRWLOCK_INIT;

          void ggml_critical_section_start(void) {
              AcquireSRWLockExclusive(&ggml_critical_section_lock);
          }

          void ggml_critical_section_end(void) {
              ReleaseSRWLockExclusive(&ggml_critical_section_lock);
          }
          '@

          if (-not $threadingSource.Contains($oldThreading)) {
            throw "Expected ggml-threading.cpp implementation was not found"
          }
          $threadingSource = $threadingSource.Replace($oldThreading, $srwThreading)
          Set-Content $threadingPath $threadingSource -Encoding UTF8

          $initPath = "src\llama_cpp\__init__.py"
          $init = Get-Content $initPath -Raw
          $oldVersion = "__version__ = `"$env:SOURCE_VERSION`""
          $newVersion = "__version__ = `"$env:PACKAGE_VERSION`""
          if (-not $init.Contains($oldVersion)) {
            throw "Expected package version $oldVersion was not found"
          }
          $init = $init.Replace($oldVersion, $newVersion)
          Set-Content $initPath $init -Encoding UTF8

          git -C src diff -- CMakeLists.txt llama_cpp/__init__.py vendor/llama.cpp/src/CMakeLists.txt vendor/llama.cpp/ggml/src/ggml-threading.cpp

      - name: Configure optimized Tiger Lake build
        shell: pwsh
        run: |
          $flags = @(
            "-DCMAKE_BUILD_TYPE=Release",
            "-DCMAKE_INTERPROCEDURAL_OPTIMIZATION=ON",
            "-DBUILD_SHARED_LIBS=OFF",
            "-DLLAVA_BUILD=OFF",
            "-DGGML_NATIVE=OFF",
            "-DGGML_OPENMP=ON",
            "-DGGML_BACKEND_DL=OFF",
            "-DGGML_CPU_ALL_VARIANTS=OFF",
            "-DGGML_CPU_REPACK=ON",
            "-DGGML_BLAS=OFF",
            "-DGGML_CUDA=OFF",
            "-DGGML_HIP=OFF",
            "-DGGML_SYCL=OFF",
            "-DGGML_OPENCL=OFF",
            "-DGGML_RPC=OFF",
            "-DGGML_METAL=OFF",
            "-DGGML_SSE42=ON",
            "-DGGML_AVX=ON",
            "-DGGML_AVX2=ON",
            "-DGGML_FMA=ON",
            "-DGGML_F16C=ON",
            "-DGGML_BMI2=ON",
            "-DGGML_AVX512=ON",
            "-DGGML_AVX512_VBMI=ON",
            "-DGGML_AVX512_VNNI=ON",
            "-DGGML_AVX512_BF16=OFF",
            "-DLLAMA_BUILD_TESTS=OFF",
            "-DLLAMA_BUILD_EXAMPLES=OFF",
            "-DLLAMA_BUILD_TOOLS=OFF",
            "-DLLAMA_BUILD_SERVER=OFF"
          )

          if ($env:ENABLE_VULKAN -eq "true") {
            $flags += "-DGGML_VULKAN=ON"
          } else {
            $flags += "-DGGML_VULKAN=OFF"
          }

          $argsLine = $flags -join " "
          "CMAKE_GENERATOR=Ninja" >> $env:GITHUB_ENV
          "CMAKE_ARGS=$argsLine" >> $env:GITHUB_ENV
          Write-Host "CMAKE_ARGS=$argsLine"

      - name: Build wheel
        shell: pwsh
        run: |
          Set-Location src
          $outDir = Join-Path $env:GITHUB_WORKSPACE "wheelhouse\$env:BUILD_VARIANT"
          New-Item -ItemType Directory -Force $outDir | Out-Null
          python -m build --wheel --outdir $outDir

      - name: Verify backend and model inference
        shell: pwsh
        run: |
          $wheel = Get-ChildItem "wheelhouse\$env:BUILD_VARIANT\*.whl" | Select-Object -First 1
          if (-not $wheel) {
            throw "No wheel was produced"
          }

          python -m venv "testenv-$env:BUILD_VARIANT"
          $testPython = Join-Path $env:GITHUB_WORKSPACE "testenv-$env:BUILD_VARIANT\Scripts\python.exe"
          & $testPython -m pip install --upgrade "pip<26"
          & $testPython -m pip install --no-cache-dir $wheel.FullName

          New-Item -ItemType Directory -Force reports | Out-Null
          New-Item -ItemType Directory -Force test-model | Out-Null
          $modelPath = Join-Path $env:GITHUB_WORKSPACE "test-model\stories15M-q4_0.gguf"
          Invoke-WebRequest -Uri $env:TEST_MODEL_URL -OutFile $modelPath
          $actualHash = (Get-FileHash $modelPath -Algorithm SHA256).Hash.ToLowerInvariant()
          if ($actualHash -ne $env:TEST_MODEL_SHA256) {
            throw "Test model SHA-256 mismatch: $actualHash"
          }

          @'
          import platform
          import sys
          from pathlib import Path

          import llama_cpp
          from llama_cpp import Llama

          model_path = Path("test-model/stories15M-q4_0.gguf").resolve()
          print("Python:", sys.version)
          print("Platform:", platform.platform())
          print("llama-cpp-python:", llama_cpp.__version__)
          print("Package:", llama_cpp.__file__)

          lib_dir = Path(llama_cpp.__file__).resolve().parent / "lib"
          print("Native library directory:")
          for path in sorted(lib_dir.iterdir()):
              print(" ", path.name, path.stat().st_size)

          print("Calling llama_backend_init()", flush=True)
          llama_cpp.llama_backend_init()
          print("BACKEND INIT OK", flush=True)
          llama_cpp.llama_backend_free()
          print("BACKEND FREE OK", flush=True)

          print("Loading smoke-test model", flush=True)
          llm = Llama(
              model_path=str(model_path),
              n_ctx=128,
              n_batch=32,
              n_threads=2,
              n_gpu_layers=0,
              verbose=True,
          )
          output = llm("Once upon a time", max_tokens=8, temperature=0.0)
          text = output["choices"][0]["text"]
          if not isinstance(text, str) or not text:
              raise RuntimeError(f"Model returned no completion: {output!r}")
          print("MODEL TEST OK:", repr(text), flush=True)
          '@ | & $testPython -I -X faulthandler -u 2>&1 |
            Tee-Object "reports\$env:BUILD_VARIANT-runtime-test.txt"

          $llamaDll = Join-Path $env:GITHUB_WORKSPACE "testenv-$env:BUILD_VARIANT\Lib\site-packages\llama_cpp\lib\llama.dll"
          if (-not (Test-Path $llamaDll)) {
            throw "llama.dll was not installed"
          }
          dumpbin /DEPENDENTS $llamaDll |
            Tee-Object "reports\$env:BUILD_VARIANT-dll-dependencies.txt"

      - name: Create reproducibility manifest
        if: always()
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force reports | Out-Null
          $manifest = "reports\$env:BUILD_VARIANT-build-manifest.txt"
          "variant=$env:BUILD_VARIANT" | Set-Content $manifest
          "package_version=$env:PACKAGE_VERSION" | Add-Content $manifest
          "source_version=$env:SOURCE_VERSION" | Add-Content $manifest
          "source_commit=$(git -C src rev-parse HEAD)" | Add-Content $manifest
          "python=$(python --version 2>&1)" | Add-Content $manifest
          "cmake_args=$env:CMAKE_ARGS" | Add-Content $manifest
          "srwlock_fix=ggml process-global std::mutex replaced with statically initialized Windows SRWLOCK" | Add-Content $manifest
          "submodules:" | Add-Content $manifest
          git -C src submodule status | Add-Content $manifest

          Get-ChildItem "wheelhouse\$env:BUILD_VARIANT\*.whl" -ErrorAction SilentlyContinue | ForEach-Object {
            "wheel=$($_.Name)" | Add-Content $manifest
            "wheel_size=$($_.Length)" | Add-Content $manifest
            "wheel_sha256=$((Get-FileHash $_.FullName -Algorithm SHA256).Hash)" | Add-Content $manifest
          }
          Get-Content $manifest

      - name: Upload wheel and reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: llama-cpp-python-${{ env.PACKAGE_VERSION }}-win-amd64
          path: |
            wheelhouse/${{ matrix.variant }}/*.whl
            reports/${{ matrix.variant }}-*.txt
          if-no-files-found: error
          retention-days: 30
